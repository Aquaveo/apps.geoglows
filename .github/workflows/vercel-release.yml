name: Trigger Vercel Deploy Hook on Release
on:
  release:
    types: [published]
jobs:
  deploy:
    if: ${{ github.event.release.prerelease == false }}
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"